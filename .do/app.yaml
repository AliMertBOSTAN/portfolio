name: portfolio
region: fra
services:
  - name: web
    github:
      branch: main
      deploy_on_push: true
      repo: AliMertBOSTAN/portfolio
    build_command: npm run build
    environment_slug: node-js
    instance_count: 1
    instance_size_slug: basic-xxs
    http_port: 8080
    routes:
      - path: /
    health_check:
      http_path: /
